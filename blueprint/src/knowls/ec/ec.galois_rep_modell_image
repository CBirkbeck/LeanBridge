\subsection{Image of mod-$\ell$ Galois representation}
\begin{definition}\label{ec.galois_rep_modell_image}
\uses{ec,ec.galois_rep,gl2.borel,gl2.exceptional,gl2.nonsplit_cartan,gl2.normalizer_nonsplit_cartan,gl2.normalizer_split_cartan,gl2.split_cartan,nf}
Let $\ell$ be a prime and let $E$ be an {{KNOWL('ec', 'elliptic curve')}} defined over a {{KNOWL('nf', 'number field')}} $K$.

Subgroups $G$ of $\GL(2,\F_\ell)$ that can arise as the image of the mod-$\ell$ {{KNOWL('ec.galois_rep', 'Galois representation')}}
\[
\bar\rho_{E,\ell}\colon {\Gal}(\overline{K}/K)\to \GL(2,\F_\ell)
\] 
attached to $E$ that do not contain $\SL(2,\F_\ell)$ are identified using the labels introduced by Sutherland in \cite{arXiv:1504.07618,MR:3482279}.  For groups with surjective determinant map (necessarily the case when $K=\Q$), these labels have the form 
\[
\texttt{LS.a.b.c},
\]
where $\texttt{L}$ is the prime $\ell$, $\texttt{S}$ is one of **G**, **B**, **Cs**, **Cn**, **Ns**, **Nn**, **A4**, **S4**, **A5**, and $\texttt{a}$, $\texttt{b}$, $\texttt{c}$ are optional positive integers.  When the determinant map is not surjective the label has "$\texttt{[d]}$", where $d$ is the index of the determinant image in $\F_\ell^\times$.

When $\bar\rho_{E,\ell}$ does not contain $\SL(2,\F_\ell)$  the possibilities for $\texttt{S}$ are: {{KNOWL('gl2.borel', 'Borel')}} **B**, {{KNOWL('gl2.split_cartan', 'split Cartan')}} **Cs**, {{KNOWL('gl2.normalizer_split_cartan', 'normalizer of the split Cartan')}} **Ns**, 
{{KNOWL('gl2.nonsplit_cartan', 'nonsplit Cartan')}} **Cn**, {{KNOWL('gl2.normalizer_nonsplit_cartan', 'normalizer of the nonsplit Cartan')}} **Nn**,  {{KNOWL('gl2.exceptional', 'exceptional')}} **A4**, **S4**, **A5**.  The cases **A4** and **A5** cannot occur when $K=\Q$.


\end{definition}


