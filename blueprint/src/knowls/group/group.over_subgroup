\subsection{Over-subgroup}
\begin{definition}\label{group.over_subgroup}
\uses{group.subgroup}
If $H \subseteq G$ is a {{KNOWL('group.subgroup', 'subgroup')}}, an **over-subgroup** is a subgroup $K \subseteq G$ with $H \subseteq K$.  A **minimal over-subgroup** is an over-subgroup $K \ne H$ that is minimal with respect to inclusion.
\end{definition}


