\subsection{Integral element of a ring}
\begin{definition}\label{ring.integral}
\uses{ring}
If $R\subseteq S$ are {{KNOWL('ring', 'commutative rings')}}, an element $s\in S$ is **integral** over $R$ if there exists $n\in\Z^+$ and $a_i\in R$ such that
$$ s^n+a_{n-1} s^{n-1}+\cdots + a_0 =0\,.$$

The **integral closure** of $R$ in $S$ is $\{s\in S\mid s \text{ is integral over } R\}$.


\end{definition}


