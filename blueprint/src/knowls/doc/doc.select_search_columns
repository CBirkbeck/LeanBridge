\subsection{Select search columns}
\begin{definition}\label{doc.select_search_columns}
\uses{doc.search_columns}
After using this input to select the {{KNOWL('doc.search_columns', 'search columns')}} shown, clicking "Search again" will reload the page and give a URL that makes the chosen columns persistent.
\end{definition}


