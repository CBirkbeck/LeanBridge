\subsection{$\Z$-conductor sum}
\begin{definition}\label{av.fq.is_zconductor_sum}
\uses{av.fq.conductor,av.fq.order}
Whether an {{KNOWL('av.fq.order', 'order')}} $R$ in an étale algebra $K$ can be written as
$ R = \Z + \mathfrak{f}_R$, 
where $\mathfrak{f}_R$ is the {{KNOWL('av.fq.conductor', 'conductor')}} of $R$ in the maximal order $\mathcal{O}_K$ of $K$.
\end{definition}


